package model;

import java.util.List;

/**
 * Represents the data model of the program.
 * <p>
 * A model class typically defines the structure of the data
 * used in the application (e.g., lists, records, or domain entities).
 * </p>
 *
 * <p>
 * In larger applications, multiple model classes may exist,
 * each representing a different concept (e.g., User, Product, Order).
 * This class currently serves as a placeholder for whichever
 * data structure (such as {@link java.util.ArrayList} or
 * {@link java.util.LinkedList}) is chosen for the program.
 * </p>
 */
public class DataModel {

    /**
     * Creates an empty data model.
     */
    public DataModel() {
        // Placeholder constructor
    }
}